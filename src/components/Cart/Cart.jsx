import React from "react";
import styled from "styled-components";

const CartContainer = styled.div`
    
`

const Cart = () => {
    return(
        <CartContainer>
            
        </CartContainer>
    );
}

export default Cart;